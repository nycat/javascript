// https://wangdoc.com/javascript/stdlib/array.html#slice
const pets = ['kido', 'maoqiu', 'xiaoxiao']

// slice(star, end) the last element is not included
var twoCats = pets.slice(0, 2)
console.log(twoCats)

var allCats = pets.slice(0)
console.log(allCats)
